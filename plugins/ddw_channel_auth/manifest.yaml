name: ddw-channel-auth
license: free
tier: beta
version: 1.0.0
display_name: DDW 渠道授权与结算
description: DDW 渠道授权与结算插件 — 账号体系 + 客户报备状态机 + 注册码实例化 + 换码广播 + 电子签框架 + 试用 + POC 报告
  + 一级分销红线
engine: '>=2.0.0'
isolation: inline
permissions:
- database.read
- database.write
dependencies:
  plugins:
    ddw-license-core: '*'
    ddw-contract-core: '*'
    ddw-signature-adapter: '*'
    ddw-lead-claim: '*'
    ddw-payment: '*'
  python:
  - reportlab>=4.0.0
  - python-docx>=1.0.0
router_prefix: /api/v1/plugins/ddw-channel-auth
