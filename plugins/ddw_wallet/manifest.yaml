name: ddw_wallet
license: commercial
tier: core
version: 0.1.0
description: DDW 预付费钱包插件 — 问渠 K12 学习 SaaS 的支付与计费底座
config:
  business:
    min_recharge_cents: 100
    max_recharge_cents: 1000000
    royalty_rates:
      default: 80
      english: 50
permissions:
- database
- network
dependencies:
  plugins:
    ddw-llm-gateway: '*'
engine: '>=2.0.0'
