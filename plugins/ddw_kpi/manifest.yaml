name: ddw-kpi
license: commercial
tier: beta
version: 0.1.0
display_name: DDW 企业 KPI
description: 培训成绩 → KPI 指标映射 + 绩效看板
engine: plugin_base
isolation: inline
permissions:
  - database.read
  - database.write
  - event_bus.subscribe
