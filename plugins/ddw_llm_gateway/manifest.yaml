name: ddw_llm_gateway
license: commercial
status: locked  # 已由 ddw_llm_gateway_plugin 取代，仅入库锁死，不部署
version: 0.1.0
description: 企业内网 LLM 网关——统一端点、智能路由、计费管控、审计日志
author: DDW Team
dependencies:
  - ddw_core
  - ddw_llm_usage  # 可选，用于复用 UsageRecord 表
  - ddw_auth       # 可选，用于统一鉴权
entry_point: plugin.py
plugin_class: Plugin
config:
  database_url: sqlite:///data/llm_gateway.db
  default_timeout_seconds: 30.0
  max_retries: 3